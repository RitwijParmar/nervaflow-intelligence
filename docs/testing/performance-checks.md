# Performance Checks

Track:

- Catalog lookup latency under broad queries.
- Scenario simulation latency with mocked and live graph backends.
- GDELT ingestion document volume and query caps.
- Fallback response time when Neo4j is unavailable.

Performance tests should report graph mode, fallback mode, and external API mocking status.
